# voiceflow-local
Local-first voice-to-clipboard desktop assistant (Tauri + React + TypeScript). Local transcription (whisper.cpp) and local rewrite (Foundry Local / Phi-4-mini-instruct). Windows-first.
